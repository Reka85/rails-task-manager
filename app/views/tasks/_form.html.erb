<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :date_of_creation %>
  <%= f.date_field :date_of_creation %>

  <%= f.label :done %>
  <%= f.check_box :done %>

  <%= f.submit %>
<% end %>
